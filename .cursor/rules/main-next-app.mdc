---
description: apps/main-next（Next 16 + React 19 App Router + BFF）目录分层与渲染策略约定
globs: apps/main-next/**/*.{ts,tsx}
alwaysApply: false
---

# apps/main-next 结构与分层

本应用为 **Next.js 16** + **React 19**、**App Router**，采用官方常见的 **`src/` 目录**：**`src/app`** 为路由与 BFF（**`src/app/api`**），其余模块与 **`src/app` 同级**，别名 **`@/*` → `src/*`**。

## Next 16 / App Router：渲染与缓存（相对优先级）

| 优先级 | 概念 | 说明 |
|--------|------|------|
| **高** | **Server Components 默认** | 无 `"use client"` 的 `page`/`layout` 尽量保持服务端组件：**敏感逻辑、大数据拼装、纯展示** 放服务端，减少下发 JS。 |
| **高** | **CSR 边界** | 仅交互、浏览器 API、Wagmi/Wujie 等必须 **`"use client"`**；客户端树尽量**小**、靠下，避免整页客户端化。 |
| **高** | **Route Handlers（`src/app/api`）** | BFF 与 cookie/头转发已在此；保持**薄代理 + 校验**，重业务在 `apps/server` 或共享包。 |
| **高** | **`loading.tsx` / `error.tsx`** | 路由级加载与错误 UI 优于页面内零散 spin；可与 Suspense 配合改善感知性能。 |
| **中** | **动态 vs 静态** | `export const dynamic`、**`fetch` 缓存** 按路由明确意图；需会话/鉴权的路由通常 **dynamic**。 |
| **低** | **Turbopack** | 开发构建以 `next dev` 默认为准。 |

**微前端**：`/audit`、`/monitor` 以 Wujie 嵌入子应用；虚拟列表规则见 **`sub-react-app`** / **`sub-vue-app`**。

## React 19（主壳 `"use client"` 区域）

在 **Wujie、Dashboard、Web3 Provider** 等客户端组件中，优先 **`startTransition`**；细节见 **`sub-react-app`**。

## `src/app/*`：路由、页面、Server Actions、BFF

| 路径 | 职责 |
|------|------|
| **`src/app/`** | **App Router**：`page`、`layout`、`loading`、`error`、`context` 等。 |
| **`src/app/api/`** | **Route Handlers（BFF）**。 |
| **`src/app/actions/`** | **Server Actions**（`"use server"`；客户端 **`@/app/actions/...`** 按文件引用）。 |

## `src/*`（与 `app` 同级）：共享模块

| 路径 | 职责 |
|------|------|
| **`src/proxy.ts`** | **Next 16 约定**：请求拦截入口（原 `middleware`）；**字面量导出 `config.matcher`**；实现委托 **`src/proxy/runProxy.ts`**。 |
| **`src/proxy/`** | **`runProxy`**、**`authRoutes`**、**`middlewareAuthNavigation`**（与 **`NextResponse`** 解耦，便于单测）。 |
| **`src/wujie/`** | **微前端宿主**、bus、**`subAppOrigins`**、Host 组件等；**`@/wujie/...`**。**勿**在此目录下放与宿主无强绑定的内容：通用 **React hook**（含经 Wujie 通信的 hook）、**骨架屏**、**全局 `.d.ts`** 分别放 **`src/hooks`**、**`src/components/skeletons`**、**`src/types`**。 |
| **`src/config/`** | **`node_service`**（`NODE_SERVICE`，BFF 上游）、**`wagmi`**、**`contracts`** 等；按需 **`@/config/...`** 或具体文件，避免客户端误拉不需要的模块时优先细粒度路径。 |
| **`src/components/`** | **`"use client"`** 通用 UI；**`skeletons/`** 含 **`AuditSkeleton`**、**`MonitorSkeleton`** 等与路由/业务相关的加载占位。**`@/components`**。 |
| **`src/hooks/`** | 仅浏览器运行的 hook；例如 **`useWeb3Sync`**（向 Wujie **`bus`** 广播 **`web3-data-change`**）与其它页面级 hook 同属此目录，**不**使用 **`src/wujie/hooks/`**。**`@/hooks/...`**。 |
| **`src/utils/`** | 浏览器工具（如 **`authFetch.ts`**）。**`@/utils/authFetch`**；勿做会间接依赖 **`@/lib`** 的宽泛 barrel。 |
| **`src/types/`** | 补充类型、**`fetch.d.ts`**、第三方模块补丁（如 **`wujie-react.d.ts`**）；**`typeRoots`** 含 **`./src/types`**。 |
| **`src/lib/`** | **`authSession`**、**`redis`**、**`bffProxy.ts`**（仅 BFF）。**`lib/index.ts`** 只导出 **`redis` / `authSession`**，**不**导出 **`bffProxy`**。**客户端禁止** **`import … from "@/lib"`**。 |

## 协作约定

- **BFF**：新增接口优先 **`src/app/api`**。
- **子应用通信**：优先 **`src/wujie`** 与 Host 模式，避免魔法字符串。
- **聚合导出**：维护 **`src/components/index.ts`**、**`src/hooks/index.ts`**、**`src/wujie/index.ts`**、**`src/proxy/index.ts`**、**`src/lib/index.ts`**（仅服务端安全符号）等；**`src/app/actions/*`** 按 **`"use server"` 文件**引用。**`bffProxy`** 对 **`@/lib/authSession`** 的直引可保留以便 Vitest mock。
