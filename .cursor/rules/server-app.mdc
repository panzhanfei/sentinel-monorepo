---
description: apps/server 的 MVC 分层与目录职责（config、client、middlewares、routes 等）
globs: apps/server/**/*.ts
alwaysApply: false
---

# apps/server 结构与分层

## `import` 路径（与目录 `index` 一致）

若某目录已有 **`index.ts` 聚合导出**（barrel），业务代码从**该目录别名**引入，**不要**再写子文件名，例如：`@/client` 而非 `@/client/prisma.client`；`@/lib` 而非 `@/lib/dualJwt`；`@/services` 而非 `@/services/user`；`@/modules/chat` 而非 `@/modules/chat/guards`；`@/workers` 而非 `@/workers/scanner`。`@/controllers/<资源>` 指向各资源子目录的 `index`，仍属聚合出口，可保留。

## 请求链路（MVC 风格）

1. **`src/routes`**：只负责挂载路径、选用中间件、绑定到 controller；不写业务逻辑。
2. **`src/controllers`**：解析请求/校验入参边界、调用 `services`（或领域 `modules`）、通过 `@/utils` 聚合导出的 `sendSuccess` / `HttpError` 等统一响应形态。
3. **`src/services`**：业务编排、对外部系统与数据层的调用；可依赖 `client` 与 `config`。

**面向页/用例的只读 DTO 聚合**（多资源合并、清洗）放在 **单一 controller 动作 + 明确命名的 service 方法** 中：优先 **一次/有限次** SQL 与 **无 N+1 循环**；可并行只读时可用受控的 `Promise.all`。**多表写入必须**在 **一个 service/模块** 内用 **Prisma 事务**（`$transaction` 等）完成，供 **一个** 路由暴露，而不是让 Next BFF 多次 HTTP 编排。

复杂或强内聚的领域逻辑可放在 **`src/modules/<领域>/`**（如 chat 的 `guards`、`stream`、领域 `service`），由 controller/service 调用，避免单文件过大。

## 目录职责（与当前仓库一致）

| 目录                  | 职责                                                                                                                                        |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| **`src/config`**      | 本服务内「客户端 / 插件 / 第三方」相关配置的统一出口；按能力分文件（如 `env`、`redis`、`ai`、`alert`），在 `config/index.ts` 等处聚合导出。 |
| **`src/client`**      | **连接型客户端单例**的集中地（如 Prisma、Redis）：初始化、导出实例，供 `services` 使用。                                                    |
| **`src/middlewares`** | Express 中间件（鉴权、异步包装、错误处理等），在 `routes` 或根路由处组合。                                                                  |
| **`src/routes`**      | 路由表；按业务分子目录（如 `user`、`chat`、`scan`），在 `routes/index.ts` 汇总。                                                            |

## 与相邻目录的边界

- **`src/utils`**：无状态通用工具（日志、统一响应封装等）。**不放**长生命周期连接对象；连接放在 `client`。
- **`src/lib`**：可复用的基础设施/协议级封装（如 JWT 工具），偏「库」而非「某一业务用例」；业务决策仍在 `services` / `modules`。
- **`src/types`**：类型扩展（如 `express.d.ts`）与仅在本服务使用的类型声明。
- **`src/workers`**：与 HTTP 生命周期独立的**后台任务 / 定时或队列消费者**；不要在路由里堆长耗时循环逻辑。

## 新增功能时的习惯

- 新资源：`routes/<资源>/` + `controllers/<资源>/` + `services/<资源>/`（按需）。
- 配置新增：在 `config` 增加独立文件，避免把第三方密钥与 URL 散落在 `services` 里。
- 可抽离到 monorepo 的「多应用共用」能力，遵循根目录项目约定中的 **`packages/`** 原则，而不是在 server 内无限膨胀。
- **聚合导出**：新增 controller、service、middleware、client、config 等**对外引用**时，同步更新对应目录或 `controllers/index.ts`、`services/index.ts`、`middlewares/index.ts`、`client/index.ts`、`config/index.ts`、`routes/index.ts`、`modules/*/index.ts` 等已有 barrel（见根目录规则「聚合导出」）。
