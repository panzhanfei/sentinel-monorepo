---
description: 前后端防线上攻击、接口安全头与配置、数据越界与算法复杂度（滑动窗口等）
alwaysApply: true
---

# 安全编码与稳健性

## 接口与 HTTP：安全响应头与配置

新增或调整 **HTTP 服务、BFF、静态资源服务** 时，应在**统一入口**（应用中间件、网关、反向代理其一）配置常见安全头，并与团队现有部署方式一致，避免各路由零散重复：

- **建议覆盖**：`Strict-Transport-Security`（HTTPS 场景）、`X-Content-Type-Options: nosniff`、`X-Frame-Options` 或 `frame-ancestors`（CSP）、`Referrer-Policy`、`Content-Security-Policy`（按业务收紧；前端构建若需内联脚本需与 CSP 策略一致）。
- **配套**：合理 **CORS**（勿 `*` 搭配凭据）、**速率限制**、**鉴权**与敏感接口的额外校验；生产环境强制 **HTTPS**。

若仓库已有安全中间件、`@sentinel/security-sdk` 或网关规则，**优先复用并扩展**，而不是在新路由里手写零散头。

## 前后端通用：减少攻击面

- **输入**：所有外部入口（query、body、header、路径参数、文件上传）做**类型与范围校验**（如 zod）；拒绝超预期长度与非法字符集。
- **注入**：数据库访问使用**参数化查询**；拼接 SQL/命令行属高危，须避免。
- **XSS**：前端慎用工 `dangerouslySetInnerHTML`；若必须，先经可信清洗；后端勿把不可信字符串当 HTML 原样输出。
- **敏感信息**：错误响应与日志中**不返回**堆栈、内部路径、密钥片段；见 `.cursor/rules/env-secrets.mdc`。

## 数据越界与数值安全

- **下标与长度**：访问数组/字符串前确认 **索引与 `length` 合法**；切片、分页参数设 **上限**（`limit`/`offset` 防 DoS）。
- **数值**：大整数或累加场景注意 **精度与溢出**（必要时 `BigInt` 或 Decimal）；`parseInt`/`Number` 前校验字符串；除法注意 **除零**。
- **时间与资源**：循环、递归设 **边界与最大深度**；避免无界队列占满内存。

## 算法：避免「硬算」，控制复杂度

在**数据规模可能很大**或**实时性要求高**时，避免 **O(n²)** 及更重、或重复全量扫描的**硬算/暴力枚举**；优先：

- **滑动窗口**、**双指针**、**前缀和/差分**、**单调栈/队列** 等将重复工作摊销掉；
- **增量更新**状态，而非每步全量重算；
- 必要时先 **估算复杂度** 与 **最坏输入规模**，再选数据结构。

若业务上只需小数据量且性能无关，可保持简单实现，但须在评审中可说明**规模上界**。
