---
description: 密码、密钥、令牌等敏感配置须放在 .env，禁止写入源码或提交仓库
alwaysApply: true
---

# 环境与密钥（`.env`）

## 必须放进环境变量文件的内容

以下**不得**硬编码在业务代码、配置文件（除示例模板外）或文档中：

- 密码、数据库连接串、私钥、证书内容
- API Key、Access Token、Refresh Token、Webhook Secret、签名密钥
- 第三方服务密钥（云厂商、支付、邮件、OSS 等）
- 其他**仅生产/预发应知**或**按环境变化**的敏感配置

## 做法

- 将实际值写在对应应用目录下的 **`.env` / `.env.local` / `.env.development`** 等（以该应用已有约定为准），通过运行时的 `process.env` 或项目统一的配置加载读取。
- **不要**把含真实密钥的文件提交到 Git；本仓库根 `.gitignore` 已忽略 `*.env` 等，仅允许 **`\.env.example`** 作为无敏感占位说明。
- 新增变量时：在 **`.env.example`**（或各应用已有的 example 文件）中增加**同名占位**与简短说明，便于他人本地复制填充。
- **不要**在 PR、issue、日志、注释里粘贴真实密钥；代码审查时若发现应要求改为环境变量引用。

## 反例与正例

```typescript
// ❌ 禁止：密钥出现在源码中
const apiKey = "sk-live-xxxx";

// ✅ 正确：从环境读取，缺省时在启动阶段校验并给出明确错误
const apiKey = process.env.MY_SERVICE_API_KEY;
```

若某能力必须「默认开发用假值」，使用**明显占位**且仅限本地/测试，并仍通过 `process.env` 注入，避免与生产密钥混在同一字面量中。
