---
description: 数据读取与关联加载时避免 N+1 查询（批量、JOIN、预加载）
alwaysApply: true
---

# 数据访问：避免 N+1

## 问题

**N+1**：先执行 **1 次**查询拿到列表（如 N 条主记录），再在循环里对**每条**记录各查 **1 次**关联数据，总查询约 **1 + N** 次，随数据量线性恶化，易拖垮 DB 与接口延迟。

## 编写与评审时的要求

- **禁止**在 `for` / `forEach` / `map` 等循环内对同一数据源发起**逐条**独立查询（除非已确认规模极小且非热路径，并显式注明）。
- 需要关联数据时，优先：
  - **一次 JOIN / 子查询** 在 SQL 层拉平；
  - **ORM 的 `include` / `with` / `eager load`** 等声明式预加载，并确认生成的是 JOIN 或有限次查询而非逐条；
  - **两次查询 + 内存组装**：先查主表 ID 列表，再用 **`WHERE id IN (...)`** 或等价 API **批量**查关联表，在内存里按 id 映射合并；
  - 图状或高度复用场景可考虑 **DataLoader** 式「按请求合并、批量取数」。

## 自检

实现列表/详情带关联字段的接口时，自问：**主查询次数 + 关联查询次数** 是否与列表长度无关（或仅常数级）？若与 **N 成正比**，应改为批量或 JOIN。
