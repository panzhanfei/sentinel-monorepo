/**
 * 兼容旧名：扫描与监控逻辑已迁至 Controller + Store + Service。
 */
export { useMonitorDashboard as useScanner } from "@/controllers/useMonitorDashboard";
export type { ChainBalance, TokenBalance } from "@/types/monitor";
