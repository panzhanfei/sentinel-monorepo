# 复制为 .env.development 等文件后按需修改（见 src/config/env.config.ts）
# 本地 Anvil，供 @sentinel/security-sdk 与扫描 worker 使用
ANVIL_RPC_URL=http://127.0.0.1:8545
# 若用仓库根目录 docker compose 起 anvil：在根目录 .env 设置 ANVIL_FORK_URL=<主网 HTTPS RPC> 可 fork 主网（勿提交密钥）
