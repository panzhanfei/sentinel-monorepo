# Monorepo 根目录：供 docker compose、根脚本（如 node:fork）使用。
# 注意：若在终端 `export $(grep ... .env)` 后跑 `pnpm dev`，所有 NEXT_PUBLIC_* 会进入子进程，
# 可能导致 main-next 在本地仍使用生产子域。本地开发建议：
# - 根 .env 里注释掉或删掉 NEXT_PUBLIC_WUJIE_REACT_URL / NEXT_PUBLIC_WUJIE_VUE_URL；或
# - 仅用 compose 需要的变量，勿整文件 export；或
# - 在 apps/main-next/.env.local 覆盖（next dev 仍会合并根目录注入的 env，故优先不要 export 生产 WUJIE）。

# SENTINEL_DOMAIN=example.com
# QUICKNODE_RPC_URL=https://...
# NEXT_PUBLIC_RPC_URL=...

# 生产微前端子域（仅部署机构建/运行环境需要；本地勿随 Turbo 注入时可不写或注释）
# NEXT_PUBLIC_WUJIE_REACT_URL=https://react.example.com
# NEXT_PUBLIC_WUJIE_VUE_URL=https://vue.example.com
